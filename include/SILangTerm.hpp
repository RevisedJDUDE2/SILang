#include <windows.h>
#include <iostream>

class SILangTerm {
  public:
    /** @brief Initializes a console / terminal enviroment interpreter.
     *  @param Title the title of the window if empty. then it will be Interpreter
     *  @returns NONE
    */
    SILangTerm(const char* Title);
};